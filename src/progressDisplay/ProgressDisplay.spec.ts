import { expect } from "chai";
import { ProgressDisplay } from "./ProgressDisplay";

describe("ProgressDisplay", () => {
    let instance: ProgressDisplay;

    beforeEach(() => {
        instance = new ProgressDisplay();
    });

});
